apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  selector:
    app: user-service
    version: blue # default to blue version
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
