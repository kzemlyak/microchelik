apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-service-blue
  namespace: social-service
  labels:
    app: social-service
    version: blue
spec:
  replicas: 10
  selector:
    matchLabels:
      app: social-service
      version: blue
  template:
    metadata:
      labels:
        app: social-service
        version: blue
    spec:
      containers:
        - name: social-service-container
          image: docker.io/library/social-service:latest
          imagePullPolicy: "Never" # for local in minikube
          ports:
            - containerPort: 8080
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1 